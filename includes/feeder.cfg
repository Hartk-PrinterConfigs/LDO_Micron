[gcode_macro FILAMENT_SENSORS]
gcode:
  {action_call_remote_method("filament_sensors_report")}


[gcode_macro FILAMENT_EVENT]
gcode:
  {% set sensor = params.SENSOR|string %}
  {% set state = params.STATE|string %}
  RESPOND PREFIX="info" MSG="FILAMENT_EVENT {sensor} -> {state}"